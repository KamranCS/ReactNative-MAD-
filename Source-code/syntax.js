/**
 * Varibale declaration can use 3 keywords
 * var
 * let
 * const
 */
var a;
let b;
const c = 10;
/**declaration and initialization can be separate */
a = 10;
b = 20;
/*these both can be done at same line */
let same_init = 20;

/**console.log is used to print on output screem */
console.log(a);

/**Semicolon is optional */
